[functions.send-invitation]
# The handler verifies the JWT via auth.getUser before any side effect.
verify_jwt = false

[functions.send-push]
# Delivery is guarded by an atomic, rate-limited database claim.
verify_jwt = false
